{
  "OllamaHost": "http://localhost:11434",
  "MetronomoMCP": {
    "Command": "dotnet",
    "Arguments": [ "run", "--project", "../MetronomoMCP" ]
  },
  "SystemPrompt": "Sei un assistente con accesso a un database SQL Server tramite MetronomoMCP. Usa i tool disponibili per rispondere alle domande sui dati: execute_query per eseguire query SQL, list_tables per elencare le tabelle, describe_table per vedere la struttura di una tabella."
}
